# Docker's embedded DNS resolver — så nginx starter selvom upstreams ikke er klar endnu
resolver 127.0.0.11 valid=30s ipv6=off;

server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # ----- Security headers -----
  add_header X-Frame-Options "SAMEORIGIN" always;
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-XSS-Protection "1; mode=block" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;

  # ----- Public health -----
  location = /health {
    default_type application/json;
    return 200 '{"status":"ok","service":"app"}';
  }

  # ----- WebSocket: /api/ws → registry:8670/api/ws (upgrade headers) -----
  # Matched before the generic /api/ block (longer prefix wins with ^~).
  location ^~ /api/ws {
    set $upstream_ws http://registry:8670;
    proxy_pass $upstream_ws;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 86400;
    proxy_send_timeout 86400;
  }

  # ----- Reverse proxy: /api/* → registry:8670/api/* (uændret path) -----
  # ^~ giver højere prioritet end regex-locations (så remoteEntry.json regex ikke matcher først)
  location ^~ /api/ {
    set $upstream_api http://registry:8670;
    proxy_pass $upstream_api;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
  }

  # ----- Reverse proxy: /host/* → host:80/* (path-prefix strippet) -----
  location ^~ /host/ {
    set $upstream_host http://host:80;
    rewrite ^/host(/.*)$ $1 break;
    proxy_pass $upstream_host;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
  }

  # ----- Dynamic remote proxies (generated by 40-runtime-config.sh + 50-ws-reload.sh) -----
  include /etc/nginx/nexus-remotes.conf;

  # ----- App's egne statiske filer -----
  location ~* remoteEntry\.(json|js)$ {
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;
    add_header Pragma "no-cache" always;
    try_files $uri =404;
  }

  location ~* /(chunk|remote)-[^/]+\.js$ {
    add_header Cache-Control "no-store" always;
    try_files $uri =404;
  }

  location ~* \.(?:css|js|woff2?|ttf|otf|eot|svg|png|jpg|jpeg|gif|webp|ico)$ {
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
  }

  # ----- SPA fallback -----
  location / {
    try_files $uri $uri/ /index.html;
  }
}
