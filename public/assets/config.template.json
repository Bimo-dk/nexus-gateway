{
  "_comment": "Genereres ved container-start fra env-vars via /docker-entrypoint.d/40-runtime-config.sh. Felter merges ind i Angular's environment-objekt før bootstrap. Override hvilken som helst environment-property runtime.",
  "hostRemoteEntry": "${HOST_REMOTE_ENTRY}",
  "hostExposedModule": "${HOST_EXPOSED_MODULE}"
}
